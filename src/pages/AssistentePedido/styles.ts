import { css } from '@emotion/react';
import styled from '@emotion/styled';

export const AssistentePedidoWrapper = styled.div`
  ${() => css`
    max-width: 1200px;
    padding-bottom: 100px;
  `}
`;
